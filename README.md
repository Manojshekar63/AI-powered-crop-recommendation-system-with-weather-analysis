## Environment

Create a `.env` file at the project root with any needed keys.

Clerk (optional authentication):

```
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
# or
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
```
